{
  "scripts": {
    "db:schema:generate": "drizzle-kit generate:sqlite --schema=db/schema.ts",
    "dev": "wrangler dev src/index.ts -e staging",
    "deploy": "wrangler deploy -e production --minify src/index.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.29.4",
    "hono": "^4.0.7",
    "jose": "^5.2.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240222.0",
    "drizzle-kit": "^0.20.14",
    "wrangler": "^3.25.0"
  }
}
